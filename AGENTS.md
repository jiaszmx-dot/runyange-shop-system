# 润阳阁云店铺 AGENTS 指南

## 1. 文件目的

这份文件用于帮助后续接手本项目的人或智能体，快速理解：

- 这个项目是做什么的
- 当前做到什么程度
- 关键业务规则是什么
- 代码主要在哪些文件
- 本地开发和预览时要注意什么

如果后续需求、数据结构、页面方向发生了明显变化，需要同步更新本文件。

---

## 2. 项目定位

本项目不是一个单店收银小工具，而是一个面向未来连锁化的“门店经营系统”。

当前业务对象是“小儿推拿艾灸馆”，目标是先跑通样本店，再复制到：

- 直营门店
- 加盟门店

系统第一阶段需要覆盖这些核心能力：

- 收银开单
- 会员管理
- 充值与储值
- 服务项目管理
- 商品与耗材管理
- 员工提成管理
- 订单与退款
- 库存与进销存基础
- 回访与会员关怀
- 活动促销与券核销
- 多店数据与权限管理
- 老板/管理员/收银员/员工多角色视角

---

## 3. 当前业务背景

### 3.1 门店场景

当前用户经营一家小儿推拿艾灸馆，这套系统要先服务样本店，再为后续连锁做标准化基础。

### 3.2 典型到店流程

目前已确认的主要流程有三类：

1. 到店充值 -> 选项目 -> 指定员工 -> 开单 -> 记录服务 -> 扣费 -> 会员回访
2. 到店咨询 -> 选项目 -> 指定员工 -> 开单 -> 记录服务 -> 支付 -> 会员回访
3. 会员到店 -> 选项目 -> 指定员工 -> 开单 -> 记录服务 -> 扣费 -> 会员回访

### 3.3 多店模式

- 直营店：总部可完整编辑
- 加盟店：总部可查看经营数据，但不能直接编辑加盟店的收银和充值
- 项目、价格、活动允许“总部下发模板，加盟店在授权范围内调整”

---

## 4. 已确认的核心业务规则

### 4.1 会员与充值

当前会员通过充值获得资格，会员价统一，不同充值档位赠送不同服务次数。

已确认的充值方案：

- 688 会员：充值 `688`，赠送 `常规盒灸 2 次`，充值提成 `5%`
- 1288 会员：充值 `1288`，赠送 `常规盒灸 4 次`，充值提成 `8%`
- 2980 会员：充值 `2980`，赠送 `常规盒灸 8 次`，充值提成 `10%`

补充说明：

- 首充、续充都可以触发充值提成
- 一个会员后续需要支持家庭成员/孩子档案

### 4.2 员工提成

- 服务提成：按项目固定金额提成
- 充值提成：按充值档位阶梯比例提成

### 4.3 扣减规则

服务项目允许按项目配置扣减方式。

目前已确认：项目可支持以下扣减方式，且可按项目差异化配置：

- 储值余额
- 赠送次数
- 项目券
- 次卡/疗程包

### 4.4 退款与权限

- 收银员退款权限：当前明确要求保留
- 退款后需要支持后续“提成反结算”的模型
- 这部分数据库模型已预留，但页面和流程还未做完整

### 4.5 活动与返现

已确认一个重要活动：

- 推荐 4 人成功充值 `1980` 后，返现 `1980`

当前理解：

- 这属于“返现奖励”而不是普通代金券
- 第一版更适合先记为“达标后生成待处理返现记录”
- 不建议第一版就强绑真实自动打款流程

### 4.6 硬件支持方向

已确认或明确希望支持：

- 扫码枪
- 热敏小票打印机
- 二维码核销

真实支付方向：

- 最终希望打通微信/支付宝真实支付
- 但第一版不能被支付资质卡死
- 可以先支持线下记账型支付方式，再升级真实支付

---

## 5. 服务与商品样本

### 5.1 服务项目分类

当前项目分为至少三类：

- 小儿项目
- 成人项目
- 特色项目

样本项目包含但不限于：

- 保健推拿
- 古法脐灸
- 常规盒灸
- 推背
- 脏腑
- 肝胆
- 元生灸
- 督灸
- 通乳

注意：

- 有按次计费项目
- 也有按小时计费项目
- 同名项目在不同分类中可能价格不同

这意味着服务项目建模不能只用“名称”唯一识别，必须考虑：

- 分类
- 计费单位
- 门店价
- 会员价
- 服务时长
- 提成

### 5.2 商品与耗材样本

当前样本包含但不限于：

- 艾条
- 穴位贴
- 食疗产品
- 药包

系统要区分：

- 商品
- 耗材

并支持：

- 库存预警
- 门店库存
- 售卖型商品
- 服务消耗型耗材

---

## 6. 前端产品方向

### 6.1 整体风格

用户已经明确视觉方向：

- 母婴安心感
- 轻奢高级感

明确不要：

- 医院感
- 传统 ERP 感
- 太花哨
- 太冷冰冰
- 字太小
- 按钮太难找

### 6.2 视觉策略

当前前端视觉方向以这些关键词为主：

- 浅灰白底
- 清爽绿色主色
- 卡片化
- 大圆角
- 信息轻一点
- 强调平板触控易用性

不要再走“品牌棕色 + 绿色硬搭”的路线。

### 6.3 收银页信息架构

用户对收银页的目标框架非常明确：

- 顶部：前台收银 / 后台管理切换
- 左侧：功能侧栏
- 中间偏左：会员、员工、支付、本单项目、结算
- 右侧：服务/商品选择区

收银操作流程固定为：

1. 搜会员
2. 选项目
3. 选员工
4. 选扣费方式
5. 确认优惠
6. 收款
7. 打小票

### 6.4 老板首页关注重点

老板首页第一优先级指标：

- 今日营收
- 充值金额
- 到店人数

第二层可继续看：

- 员工业绩
- 待回访会员
- 热门项目
- 库存预警
- 多店数据

---

## 7. 当前技术栈

### 7.1 基础技术

- `Next.js 14.2.35`（App Router）
- `React 18`
- `TypeScript`
- `Prisma 5`
- `PostgreSQL`

### 7.2 依赖特征

当前没有引入复杂组件库，页面主要靠：

- 原生 React 组件
- 全局 CSS
- 本地 mock 数据

这意味着：

- 改界面速度快
- 但组件复用体系还未真正搭起来

---

## 8. 目录结构

项目根目录：`C:\Users\Administrator\Documents\润阳阁云店铺`

关键目录：

- `docs/`：项目文档、业务规则、前端风格说明
- `prisma/`：数据库 schema 和 seed
- `src/app/`：Next.js 路由页面
- `src/components/`：页面级组件
- `src/lib/`：业务规则、mock 数据、数据库封装、导航配置

关键文件：

- `docs/store-system-brief.md`
- `docs/store-system-blueprint-v1.md`
- `docs/store-system-business-rules-template.md`
- `docs/frontend-style-ia-brief.md`
- `prisma/schema.prisma`
- `prisma/seed.ts`
- `src/lib/business-rules.ts`
- `src/components/console-shell.tsx`
- `src/components/cashier-workbench.tsx`
- `src/app/globals.css`

---

## 9. 当前页面与实现状态

### 9.1 已有页面路由

- `/`
- `/console`
- `/console/cashier`
- `/console/employee`
- `/console/growth`
- `/console/master-data`
- `/console/staff`

### 9.2 页面成熟度

当前状态更接近“高保真原型 + 前端样板”，不是完整业务系统。

大致分层如下：

- `/console/cashier`：前台收银工作台，当前迭代最多
- `/console`：老板经营看板样板
- 其他 `/console/*` 页面：更多是结构占位和产品方向样板

### 9.3 数据来源现状

当前页面主要使用两类数据：

1. `src/lib/business-rules.ts`
   - 业务规则型静态数据
   - 用于 seed 和业务基线

2. `src/lib/console-demo.ts`
   - 经营看板和演示页面用的 mock 数据

说明：

- 当前大部分页面还没有真正接数据库读取
- 收银页当前也是前端状态流驱动，不是真正订单提交

---

## 10. 数据库建模现状

`prisma/schema.prisma` 已经搭了比较完整的业务模型，覆盖了：

- 组织 Organization
- 门店 Store
- 用户 User
- 用户权限 UserAccess
- 会员 Member
- 家庭成员 FamilyMember
- 充值方案 RechargePlan
- 充值赠送 RechargePlanGift
- 服务分类 / 服务项目
- 商品分类 / 商品
- 门店价格
- 库存 / 库存流水
- 会员账户 / 账户流水
- 疗程包 / 次卡授予
- 订单 / 订单明细 / 分配 / 结算
- 退款
- 提成
- 活动
- 推荐返现规则
- 券
- 回访任务 / 回访记录
- 审计日志

这一层建模方向是对的，明显是按“可连锁复制”思路设计的，不是随手做单店账本。

---

## 11. Seed 与基础数据

`prisma/seed.ts` 已经会根据 `src/lib/business-rules.ts` 初始化：

- 组织
- 总店
- 服务分类与项目
- 商品分类与商品
- 服务结算策略
- 门店价格
- 库存记录
- 充值方案与赠送规则
- 活动
- 用户与角色访问权限

注意：

- 当前种子更像“演示数据初始化”
- 真正投入使用前，仍要做真实基础资料录入流程

---

## 12. 本地运行方式

### 12.1 常用命令

- 启动开发：`npm run dev`
- 构建：`npm run build`
- 启动生产预览：`npm run start`
- Lint：`npm run lint`
- Prisma 生成：`npm run db:generate`
- 推送 schema：`npm run db:push`
- 数据库迁移：`npm run db:migrate`
- 种子数据：`npm run db:seed`

### 12.2 环境变量

示例见 `.env.example`：

- `DATABASE_URL`

当前模板是 PostgreSQL。

### 12.3 数据库连接说明

只有配置好真实 `DATABASE_URL` 后，数据库能力才真正可用。

如果只是看前端页面，数据库不一定是必须的；但：

- Prisma build
- seed
- 后续真实 CRUD

最终都依赖 PostgreSQL。

---

## 13. 当前已知问题与开发注意事项

### 13.1 Next 本地缓存问题

这个项目在当前 Windows 环境下，出现过多次 Next 构建/运行时缓存异常，典型报错：

- `Cannot find module './948.js'`

根因表现为：

- `.next` 缓存脏掉
- 服务端 chunk 引用断链
- 预览页突然 500 或加载红屏

遇到这类问题的优先处理方式：

1. 停掉本地服务
2. 删除 `.next`
3. 重新 `npm run build`
4. 再 `npm run start`

### 13.2 当前预览建议

当前更稳的方式是：

- 修改代码后 `build`
- 再用 `next start` 预览

因为 `next dev` 在这台机器上曾多次出现 HMR/静态资源缓存异常。

### 13.3 中文显示问题

在某些终端输出里，中文可能出现乱码；这通常是终端编码显示问题，不一定代表源码本身损坏。

如果页面浏览器里中文正常，不要仅凭 shell 输出就误判文件坏掉。

---

## 14. 当前最重要的前端文件

### 14.1 收银页

- `src/components/cashier-workbench.tsx`
- `src/app/console/cashier/page.tsx`
- `src/app/globals.css`

这里是最近迭代最频繁、最容易出布局问题的区域。

### 14.2 控制台壳层

- `src/components/console-shell.tsx`

说明：

- `/console/cashier` 会走独立收银壳层
- 其他后台页走常规控制台壳层

### 14.3 老板看板

- `src/app/console/page.tsx`
- `src/lib/console-demo.ts`

---

## 15. 当前最重要的产品判断

这套系统第一版应该优先做到：

- 核心业务闭环完整
- 页面可实际操作
- 数据结构正确
- 为多店和连锁扩展留好底

而不是一开始就把所有高级能力做满。

更具体地说：

- 真实支付可以后接
- 高级 BI 可以后做
- 复杂营销自动化可以后做
- 复杂打印模板系统可以后做

但这些不能影响：

- 收银闭环
- 会员闭环
- 订单与结算闭环
- 项目/商品/提成/库存基础闭环

---

## 16. 继续开发时的优先顺序建议

如果继续推进，推荐优先级如下：

1. 先把收银页结构和交互体验彻底磨顺
2. 再把收银页接成真实状态流
3. 再做登录 / 角色权限 / 基础资料 CRUD
4. 然后接数据库读写
5. 再做退款、券核销、回访任务
6. 最后再碰真实支付与硬件联调

---

## 17. 一句话总结

这是一个面向小儿推拿艾灸馆连锁化经营的 Next.js + Prisma 门店系统项目，当前数据库建模基础较完整，前端正处于“高保真收银工作台 + 管理后台样板”阶段，收银页是当前开发核心，运行时最需要小心的是 Next 的 `.next` 缓存异常问题。
